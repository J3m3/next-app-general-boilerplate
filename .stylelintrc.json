{
  "extends": "stylelint-config-standard-scss",
  "rules": {
    "selector-class-pattern": [
      "^([a-z][a-z0-9]*)((-{1,2}|_{2})[a-z0-9]+)*$",
      { "message": "Expected class selector to be kebab-case or satisfy bem structure" }
    ]
  }
}
